name: fm_music
description: Music search app using last.fm’s API.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.4
  retrofit: ^3.0.1+1
  cached_network_image: ^3.2.0
  flutter_bloc: ^8.0.1
  equatable: ^2.0.3
  get_it: ^7.2.0
  bloc_concurrency: ^0.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  build_runner: ^2.1.7
  retrofit_generator: ^4.0.1


flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/
